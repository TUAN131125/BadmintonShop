<style>
    .chatbot-btn {
        position: fixed;
        bottom: 90px;
        right: 20px;
        width: 55px;
        height: 55px;
        border-radius: 50%;
        background: linear-gradient(135deg, #ff6b01, #ff9f43);
        color: white;
        border: none;
        box-shadow: 0 4px 12px rgba(255, 107, 1, 0.4);
        z-index: 9999;
        font-size: 24px;
        cursor: pointer;
        transition: all 0.3s cubic-bezier(0.175, 0.885, 0.32, 1.275);
        display: flex;
        align-items: center;
        justify-content: center;
    }

        .chatbot-btn:hover {
            transform: scale(1.1) translateY(-3px);
            box-shadow: 0 8px 20px rgba(255, 107, 1, 0.6);
        }

        .chatbot-btn::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            border-radius: 50%;
            border: 2px solid #ff6b01;
            opacity: 0;
            animation: pulse-ring 2s infinite;
        }

    @@keyframes pulse-ring {
        0% {
            transform: scale(0.8);
            opacity: 0.8;
        }

        100% {
            transform: scale(1.5);
            opacity: 0;
        }
    }
</style>

<button class="chatbot-btn" title="Chat với chúng tôi">
    <i class="fa-solid fa-comments"></i>
</button>